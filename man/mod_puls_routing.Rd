% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modpuls.R
\name{mod_puls_routing}
\alias{mod_puls_routing}
\title{Modified Puls Reservoir Routing}
\usage{
mod_puls_routing(resmodel_df, inflow_df, initial_elev, full_results = FALSE)
}
\arguments{
\item{resmodel_df}{Data frame with three columns: elevation/stage (ft),
storage (acre-feet), and discharge (cfs). Must be in that order.}

\item{inflow_df}{Data frame with two columns: time (hours) and
inflow (cfs). Must be in that order.}

\item{initial_elev}{Starting water surface elevation in feet.}

\item{full_results}{Logical. If \code{FALSE} (default), returns only peak
stage and discharge. If \code{TRUE}, returns the complete routing result.}
}
\value{
If \code{full_results = FALSE}, a named numeric vector with
\code{peak_stage_ft} and \code{peak_discharge_cfs}.
If \code{full_results = TRUE}, a data frame with columns: \code{time_hr},
\code{inflow_cfs}, \code{elevation_ft}, \code{storage_acft}, and \code{outflow_cfs}.
}
\description{
Performs Modified Puls (level pool) routing of inflow hydrograph given
a defined reservoir geometry (Stage (ft), Storage (ac-ft), Discharge (cfs)).
}
\examples{

# Example hydrograph. Requires pre-processing
hydro_example <- hydrograph_setup(jmd_hydro_jun1965_15min, critical_duration = 2, routing_days = 10)
hydrograph_shape <- hydro_example[[1]][, 2:3]
scaled_hydrograph <- scale_hydrograph(hydrograph_shape,
                                      observed_volume = 50000,
                                      sampled_volume = 55000)

# Peak values only
mod_puls_routing(jmd_resmodel, scaled_hydrograph, initial_elev = 3830)

# Full routing table
jmd_full_routing <- mod_puls_routing(jmd_resmodel, scaled_hydrograph, initial_elev = 3830, full_results = TRUE)
head(jmd_full_routing)

}
\references{
Chow, V.T. (1959). Open-Channel Hydraulics. McGraw-Hill.
}

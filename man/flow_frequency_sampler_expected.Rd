% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow_frequency_sampler_expected.R
\name{flow_frequency_sampler_expected}
\alias{flow_frequency_sampler_expected}
\title{Flow Frequency Sampler (Expected Only)}
\usage{
flow_frequency_sampler_expected(
  bestfit_params,
  freq_dist = "LP3",
  strat_dist = "ev1",
  Nbin = NULL,
  Mevent = NULL
)
}
\arguments{
\item{bestfit_params}{Data frame of distribution parameters from RMC-BestFit.
Must have \code{Nbin * Mevent} rows (one parameter set per z-ordinate).
For LP3: columns are mean (log), sd (log), skew (log).
For GEV: columns are location, scale, shape.}

\item{freq_dist}{Character. Distribution type. Either \code{"LP3"} (default) or \code{"GEV"}.}

\item{strat_dist}{Character. Probability space for stratification bins.
Passed to \code{\link[=stratified_sampler]{stratified_sampler()}}. One of \code{"ev1"} (default), \code{"normal"},
or \code{"uniform"}. See \code{\link[=stratified_sampler]{stratified_sampler()}} for details.}

\item{Nbin}{Integer. Number of stratified bins. Default is \code{50}.}

\item{Mevent}{Integer. Number of events per bin. Default is \code{200}.}
}
\value{
A list containing:
\describe{
\item{flow}{Matrix of sampled flow values \verb{[Mevent x Nbin]}}
\item{nbins}{Number of stratified bins}
\item{mevents}{Number of events per bin}
\item{weights}{Probability weights for each bin from \code{\link[=stratified_sampler]{stratified_sampler()}}}
}
}
\description{
Generates a stratified matrix of flow values by pairing each z-ordinate with
a different parameter set from the posterior distribution. This collapses the
nested Monte Carlo structure into a single pass, simultaneously sampling
natural variability (via stratified z-ordinates) and knowledge uncertainty
(via varying parameters). Used internally by \code{\link[=rfa_simulate]{rfa_simulate()}} for expected-only
mode.
}
\examples{
# Expected only using all 10,000 parameter sets
result <- flow_frequency_sampler_expected(jmd_bf_parameter_sets,
                                          Nbin = 20, Mevent = 500)
dim(result$flow)  # 500 x 20
jmd_result <- flow_frequency_sampler_expected(
  jmd_samples$params,
  freq_dist = "LP3",
  Nbin      = 20,
  Mevent    = 500)

# Using bootstrapped parameter samples
jmd_samples <- bootstrap_vfc(
  c(jmd_vfc_parameters$mean_log,
    jmd_vfc_parameters$sd_log,
    jmd_vfc_parameters$skew_log),
  dist = "LP3",
  ERL  = jmd_vfc_parameters$erl)

jmd_result <- flow_frequency_sampler_expected(
  jmd_samples$params,
  freq_dist = "LP3",
  Nbin      = 20,
  Mevent    = 500)
}
\seealso{
\code{\link[=stratified_sampler]{stratified_sampler()}}, \code{\link[=qp3]{qp3()}}, \code{\link[=flow_frequency_sampler]{flow_frequency_sampler()}}, \code{\link[=rfa_simulate]{rfa_simulate()}}
}

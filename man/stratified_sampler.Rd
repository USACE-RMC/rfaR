% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stratified_sampler.R
\name{stratified_sampler}
\alias{stratified_sampler}
\title{Stratified Sampler}
\usage{
stratified_sampler(
  minAEP = 1e-08,
  maxAEP = 0.99,
  dist = "EV1",
  Nbins = NULL,
  Mevents = NULL,
  verbose = FALSE
)
}
\arguments{
\item{minAEP}{Minimum annual exceedance probability. Default is \code{1E-8}.}

\item{maxAEP}{Maximum annual exceedance probability. Default is \code{0.99}.}

\item{dist}{Character. Probability space for stratification. Case-insensitive.
Default is \code{"EV1"}. Invalid values trigger a warning and default to \code{"EV1"}.
\describe{
\item{\code{"EV1"}}{Extreme Value Type I (Gumbel) space. Recommended for flood
frequency analysis. Allocates more bins to rare events through the
transformation \code{-log(-log(1-AEP))}, improving tail estimation efficiency.}
\item{\code{"Normal"}}{Standard normal (z-score) space. Uniform bins in z-space.
Use when the underlying phenomenon is normally distributed.}
\item{\code{"Uniform"}}{Uniform probability space. Equal probability width per bin.
Generally inefficient for rare event estimation.}
}}

\item{Nbins}{Number of stratified bins. Default is \code{20}.}

\item{Mevents}{Number of events per bin. Default is \code{500}.}

\item{verbose}{Logical. If \code{TRUE}, prints a completion message summarizing
the stratification. Default is \code{FALSE}.}
}
\value{
A list containing:
\describe{
\item{normOrd}{Vector of standard normal ordinates spanning all bins}
\item{Weights}{Vector of probability weights for each bin}
\item{Zlower}{Vector of lower bounds for each bin (standard normal)}
\item{Zupper}{Vector of upper bounds for each bin (standard normal)}
\item{Nbins}{Number of bins}
\item{Mevents}{Number of events per bin}
}
}
\description{
Creates stratified bins in standard normal space for use in stratified
sampling. Stratification improves sampling efficiency by
ensuring adequate coverage of rare events.
}
\details{
The function divides the probability space into bins using the specified
transformation. EV1 transformation is recommended for heavy-tailed distributions
common in flood frequency analysis, as it naturally allocates more sampling
effort to rare events critical for dam safety assessments.
}
\examples{
# Default stratification
strat <- stratified_sampler()

# Custom bins and events
strat <- stratified_sampler(minAEP = 1E-6,
                            maxAEP = 0.5,
                            dist = "EV1",
                            Nbins = 10,
                            Mevents = 100)

# With verbose message
strat <- stratified_sampler(verbose = TRUE)

}
